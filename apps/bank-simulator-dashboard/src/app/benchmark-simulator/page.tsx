import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function BenchmarkSimulatorPage() {
  return (
    <PhasePlaceholder
      title="Benchmark Simulator"
      subtitle="Record a simulated Central Bank benchmark change as its own record — it never auto-updates borrower contracts."
      plannedIn="Phase 5"
    />
  )
}
