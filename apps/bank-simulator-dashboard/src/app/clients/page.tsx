import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function ClientsPage() {
  return (
    <PhasePlaceholder
      title="Clients"
      subtitle="Allowlisted test-client directory with product mix, commitments, and data-completeness state."
      plannedIn="Phase 2"
    />
  )
}
