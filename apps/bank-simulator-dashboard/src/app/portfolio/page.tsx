import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function PortfolioPage() {
  return (
    <PhasePlaceholder
      title="Portfolio"
      subtitle="Distributions by type, balance, rate, provenance, and data completeness — text summaries and tables alongside restrained charts."
      plannedIn="Phase 5"
    />
  )
}
