import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function ActivityLogPage() {
  return (
    <PhasePlaceholder
      title="Activity Log"
      subtitle="Demo activity log — append-only record of campaign, calculation, insight, and email events. No PII, balances, or secrets."
      plannedIn="Phase 4"
    />
  )
}
