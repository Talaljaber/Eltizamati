import { PhasePlaceholder } from '@/components/phase-placeholder'

export default function CommunicationsPage() {
  return (
    <PhasePlaceholder
      title="Communications"
      subtitle="Demo email outbox — preview, dev-sink, or Gmail SMTP delivery, allowlist-gated recipients only."
      plannedIn="Phase 4"
    />
  )
}
