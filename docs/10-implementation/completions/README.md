# Phase Completion Reports

One report per phase, named `PHASE-N-COMPLETION.md`, filed **before** the next phase may start (global gate 14, [IMPLEMENTATION_PLAN.md §9](../../08-delivery/IMPLEMENTATION_PLAN.md)).

Each report must contain **real verification evidence**, not claims: commands run with outputs, test counts, coverage numbers, device/screenshot/recording evidence where the phase requires it, the decision/cut log, and `ASSUMPTION:` / `DOC-ISSUE:` lines per AI_AGENT_RULES §D.

No reports exist yet — no phase has completed. (Phase 1 is In Progress; see [STATUS.md](../STATUS.md).)
