{
  "name": "@eltizamati/finance-engine",
  "version": "0.0.1",
  "private": true,
  "description": "Pure TypeScript finance engine — no I/O, no React, no SQLite. Deterministic given inputs.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@eltizamati/domain": "workspace:*",
    "decimal.js": "^10.5.0"
  },
  "devDependencies": {
    "@eltizamati/demo-data": "workspace:*",
    "@vitest/coverage-v8": "^3.2.4",
    "fast-check": "^4.1.1",
    "vitest": "^3.2.4"
  }
}
